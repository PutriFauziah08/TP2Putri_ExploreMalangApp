// Root build.gradle file
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.3.1' // Update to a compatible version with compileSdk 34
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
